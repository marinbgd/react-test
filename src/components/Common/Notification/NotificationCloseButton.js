import React from 'react';
import styles from './Notification.scss';

const CloseButton = () => <span className={`${styles['Button__close']} material-icons`}>close</span>;

export default CloseButton;
